<Page x:Class="DesktopApp.Pages.Page8"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page8">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Day-wise Activities Planner"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,10"/>
        <!-- Save Activities Button -->
        <Button x:Name="SaveActivitiesButton"
        Content="Save Activities"
        HorizontalAlignment="Right"
        VerticalAlignment="Top"
        Margin="10"
        Padding="10"
        Click="SaveActivitiesButton_Click"/>

        <!-- Tab Control -->
        <TabControl x:Name="DayTabs" Grid.Row="1" Margin="10">
            <!-- Tabs dynamically populated in code-behind -->
        </TabControl>
    </Grid>
</Page>
